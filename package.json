{
  "name": "optimal-select",
  "description": "Get efficient & robust CSS selectors for HTML elements",
  "version": "4.0.1",
  "author": "Stefan Dühring | Autarc <autarc@gmail.com>",
  "bugs": {
    "url": "https://github.com/Autarc/optimal-select/issues"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  },
  "homepage": "https://github.com/Autarc/optimal-select",
  "keywords": [
    "css",
    "path",
    "selector",
    "element"
  ],
  "license": "MIT",
  "main": "lib/index.js",
  "module": "src/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Autarc/optimal-select.git"
  },
  "scripts": {
    "build": "esbuild src/index.mjs --bundle --outfile=dist/optimal-select.js --format=iife --global-name=OptimalSelect && esbuild src/index.mjs --bundle --outfile=dist/optimal-select.min.js --format=iife --global-name=OptimalSelect --minify"
  }
}
